@model Topic

<div class="card border-primary m-1 p-1">
    <div class="bg-light p-1">
        <h4>
            @Model.Title
        </h4>
        <span class="card-text p-1">
            @Model.Body
        </span>
    </div>
</div>
